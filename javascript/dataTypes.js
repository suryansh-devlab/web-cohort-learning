// Primitive Data Type

// string - "", '', ``
let name = "Suryansh";

// Number - integer, float, double
let age = 23;

// Boolean - true, false
let isPaid = true;

// null - The variable exists but intentionally has no value.
let favouriteClass = null;

// undefined → variable declared but not assigned
let hometown;

// Non -Primitive Data Type

//  Object
let skills = ["HTML", "CSS", "JavaScript"];

// object -> Key value pairs
let studentProfile = {
  name: "Surya",
  age: 23,
  isPaid: true,
  skills: [`HTML, CSS, JAVASCRIPT`],
  favouriteClass: null,
  hometown,
};

console.log(typeof age); // typeof -> is used to determinr the which type of data
